ignore:
  - 'dist/.*'
coverage:
  status:
    patch: off
    project:
      default:
        target: 95%
        threshold: 2%
        base: auto
comment:
  layout: 'header, diff, files'
  behavior: default
  require_changes: false
  hide_project_coverage: false
